import {Text, View} from "react-native";

type UserCredentials = {
    email: string;
    password: string;
};

const LoginScreen = () => {
    
    return (<View></View>);
}

export default LoginScreen;